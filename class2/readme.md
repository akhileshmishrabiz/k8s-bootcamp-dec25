# running flask app (minikube) + postrges (rds)

- docker image build (first use a local image)
- load the image to minikub env 
- run the app as deployemnet + service
- see logs/event to foix failures
- confogure secret and all


- docker image from ecr (use the image from ecr)






code  ~/.kube/config

minikube image load student-portal